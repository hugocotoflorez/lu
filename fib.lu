import libc.stdio;

main(args[])
{
        assert args.count >= 1
        int n = args[1];
        // assume that n > 0
        int a = 1;
        int b = 1;
        int c;
        while(n--) {
                printf("%d\n", a);
                b <> a; // b=a and a=b
                b += a;
        }
}
